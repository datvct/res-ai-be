# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=root
DB_DATABASE=redis_ai_db

# JWT Configuration
JWT_SECRET=super_access_secret_change_me
JWT_EXPIRES_IN=30d

# File Upload Configuration
# Local development: Comment out or leave empty to use ./uploads
# UPLOAD_DIR=
# Production (Docker/Server): Uncomment and set absolute path
# UPLOAD_DIR=/opt/app/uploads
